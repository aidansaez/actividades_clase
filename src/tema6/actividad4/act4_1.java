package tema6.actividad4;

public class act4_1 {
// a. No, una interfaz no puede incluir variables de instancia, solo constantes.

// b. La herencia permite heredar métodos y propiedades de una clase, mientras que una interfaz define un contrato sin implementación. Una clase hereda de una sola clase, pero puede implementar múltiples interfaces.

// c. Los métodos en una interfaz no pueden ser privados porque deben ser accesibles para las clases que los implementan.

// d. No, las interfaces no pueden tener varias definiciones de la misma constante; cada constante debe tener un nombre único.

// e. Sí, una clase puede heredar de varias interfaces, permitiendo implementar múltiples comportamientos.

// f. No habrá error si una clase implementa varias interfaces con el mismo método, ya que la clase debe proporcionar su propia implementación. No hay conflicto si las firmas son idénticas.
}
